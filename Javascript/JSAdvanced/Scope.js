/* Scope: phạm vi
- Các lại phạm vi:
  + Global: toàn cục - bất cứ đâu cũng sử dụng được
  + Code block: khối mã - let, const - chỉ truy cập được bên trong khối mã { }
  + Local scope: hàm - var, function - chỉ sử dụng được biến đó trong hàm đó, 
                                       chứ hàm bên trong hàm đó không sử dụng được =))
- Khi gọi mỗi hàm luôn có một phạm vi mới được tạo
- Các hàm có thể truy cập các biến được khai báo trong phạm vi của nó và bên ngoài nó
- Các thức một biến được truy cập 
*/
